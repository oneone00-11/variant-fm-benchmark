# Analysis / reproduction stack (CPU-only, no GPU needed).
# Versions below are the ones the results were reproduced with (Python 3.9).
# `pip install -r requirements.txt` is all you need to run scripts/reproduce_main.py.
pandas==2.3.3
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
matplotlib==3.9.4
requests==2.32.5
tqdm==4.68.3
pyarrow==21.0.0
python-docx==1.2.0
# Test runner for the pytest guardrail suite (tests/ + phase1/tests/, see pytest.ini).
pytest==8.4.2

# -----------------------------------------------------------------------------
# OPTIONAL — only needed to RE-SCORE models (not needed to reproduce results;
# the final scores already ship in data/processed/score_matrix_final.tsv):
#   pysam            # GPN-MSA remote tabix query  (scripts/90_score_gpn.py)
#   alphagenome      # AlphaGenome API client      (scripts/91_score_alphagenome.py)
# SpliceAI / Pangolin were run in a separate conda env (see refs/*.sh, README).
