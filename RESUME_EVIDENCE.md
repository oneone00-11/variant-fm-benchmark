# Where the evidence-strength rework stands, and the one command to continue

Branch `evidence/rework`, not pushed. Everything below is committed except where noted.

## What is finished and on disk

| stage | state |
|---|---|
| E0 Walker transcription | `phase1/config/walker2023.yaml` — done, with its unverified items named |
| E0 column check | done: version/statistic/masking agree, distance does not (-D 50 vs -D 4999) |
| E1 analysis set | `phase1/data/evidence/analysis_set_v1.parquet`, 8,853 SNVs; disposition gate 1,774 + 7 = 1,781 |
| E2 / E3 / E4 | run, on the distance-50 SpliceAI column — must be re-run once the Walker column exists |
| E5 AVI | blocked, not startable here (needs `alphagenome>=0.9.0` + an API key) |
| E6 | subsets written, both event scorers written, events not yet scored |
| E7 TP53 | applied |
| E7 DDX3X | prepared, mapped, 5 columns scored, Walker cut points applied |
| E8 | `scripts/reproduce_evidence.py`, 17 new tests (117 pass), `docs/evidence-delta.md` |
| report | `~/Desktop/E1_evidence-strength补实验_执行报告_20260917.md`, generated by script from the tables |

## The one thing still to do

The Walker-basis SpliceAI column for the seven genes. It was 7,750 of 8,853 done
when the previous session ended and the process was killed; the scorer wrote only
at the end, so that run was lost. It now checkpoints every 250 variants and a
re-run resumes from the checkpoint, so an interruption costs at most a few minutes.

Run this first:

```bash
cd ~/work/variant-fm-benchmark
~/Documents/kimi/workspace/functional-standard-atlas/models/spliceai/.venv/bin/python \
  phase1/src/evid_score_spliceai_walker.py \
  --variants phase1/data/evidence/analysis_set_variants.parquet --run
```

About 2.5 hours from cold on this machine, alone. Safe to kill and restart.

Then the rest, which takes about an hour:

```bash
# TP53 on the same basis (4 min)
~/Documents/kimi/workspace/functional-standard-atlas/models/spliceai/.venv/bin/python \
  phase1/src/evid_score_spliceai_walker.py \
  --variants phase1/data/evidence/external/tp53_variants.parquet \
  --out phase1/data/evidence/external/tp53_spliceai_walker.parquet --run

cd phase1
PYTHONPATH=. ../.venv/bin/python -m src.evid_build_set        # merges the new column
PYTHONPATH=. ../.venv/bin/python -m src.evid_inframe --subset # E6 subsets, then the
                                                              # two event scorers it prints
PYTHONPATH=. ../.venv/bin/python -m src.evid_walker_thresholds
PYTHONPATH=. ../.venv/bin/python -W ignore -m src.evid_interval_lr     # ~40 min
PYTHONPATH=. ../.venv/bin/python -W ignore -m src.evid_territory_metrics
PYTHONPATH=. ../.venv/bin/python -m src.evid_delta
PYTHONPATH=. ../.venv/bin/python -m src.evid_external --merge-scores ddx3x
PYTHONPATH=. ../.venv/bin/python -m src.evid_external --apply TP53
PYTHONPATH=. ../.venv/bin/python -m src.evid_external --apply DDX3X
PYTHONPATH=. ../.venv/bin/python -m src.evid_inframe --attribute
```

`python scripts/reproduce_evidence.py` runs the analysis stages in that order; the model
re-scores are the steps it cannot do for you, and it names the command for each when
its input is missing.

## Not done, with the reason

- E5 AVI — needs `alphagenome>=0.9.0` (pinned env is 0.7.0, no atlas module) and an
  API key that is not on this machine. `src/evid_score_avi.py --check` prints the path.
- DDX3X `pangolin` / `alphagenome` / `nt` columns — Pangolin needs a gffutils database
  covering chrX; AlphaGenome shares the AVI blocker; NT is ~1 h CPU for the tool that
  reaches no tier at 11-50 bp.
- Per-item page numbers in `walker2023.yaml` — the publisher blocks full text from this
  machine. Thresholds were transcribed from the preprint full text and the table number
  confirmed against the published PMC record.
- Evidence-band edges at priors 0.05 / 0.20 — Tavtigian publishes a solution at 0.10
  only, so the report gives posteriors at the three priors instead.
