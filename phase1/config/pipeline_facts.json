{
 "note": "Quantities the manuscript states that appear inside no reports/ file. The disclosed test count is one: it is measured, not looked up, and it went stale once already -- the manuscript said thirty-three while a bare clone ran forty-seven. Since the frozen-matrix-v2 revision it is measured from a `git archive` extract of the release commit, which is what the Zenodo deposit contains, rather than from a clone. tests/test_reported_numbers.py compares the manuscript against this file.",
 "guardrail_tests_passing_from_bare_clone": 63,
 "bare_clone_measured_for": "HEAD at 5099eb3 plus this round's fixes: the twelve stages added to reproduce_calibration.py, the reproduction-coverage gate, and the committed fixture that replaced the pre-fix-draft skip; no configuration, no API keys",
 "bare_clone_breakdown": {
  "measured_at": "2026-09-09",
  "python": "3.9.6 (system python3, matching the pinned floor)",
  "install": "pip install -r requirements.txt",
  "isolation": "HOME pointed at an empty directory, so no manuscript is visible",
  "collected": 66,
  "passed": 63,
  "failed": 0,
  "skipped": 3,
  "skips_are": "the three tests that read the manuscript: the two anchor-resolution tests and the disclosed-count check. The pre-fix-draft regression no longer skips: it runs against tests/fixtures/pre_fix_clinical_yield.docx, which is tracked, so it no longer depends on a backup on the author's machine",
  "with_manuscript_visible": {
   "collected": 66,
   "passed": 66,
   "failed": 0,
   "skipped": 0
  }
 },
 "guardrail_tests_passing_from_archive": 81,
 "archive_measured_for": "git archive of the code commit of the rounding-consistency round (tables stored unrounded, supplement printed at a precision that rounds back, cross-document tests); re-measured on the manuscript-support commit that follows it, which adds no tests",
 "archive_breakdown": {
  "measured_at": "2026-09-14",
  "how": "git archive --format=tar HEAD | tar -x; python -m pytest -q -rs from the extract root with the repository's pinned virtualenv (python 3.12.14; pip install -r requirements.txt plus pyarrow and python-docx, which the tests import); HOME pointed at an empty directory and VARIANT_FM_MANUSCRIPT / VARIANT_FM_SUPPLEMENT unset, so neither document is visible",
  "collected": 91,
  "passed": 81,
  "failed": 0,
  "skipped": 10,
  "skips_are": [
   "tests/test_analysis_claims.py::test_every_anchor_hits_exactly_once -- manuscript not available",
   "tests/test_analysis_claims.py::test_paragraph_numbers_are_not_load_bearing -- manuscript not available",
   "tests/test_reported_numbers.py::test_the_disclosed_test_count_is_the_measured_one -- manuscript not available",
   "tests/test_reported_numbers.py::test_table_1_rounds_the_unrounded_pooled_values_once -- manuscript not available",
   "tests/test_reported_numbers.py::test_table_3_rounds_the_calibration_summary_once -- manuscript not available",
   "tests/test_cross_document_rounding.py (4 tests: Table 1, Table 3, the Figure 2 caption and the 3.4 sentence against the supplement) -- manuscript or supplement not available",
   "tests/test_reproduction_coverage.py::test_every_tracked_result_has_a_documented_entry_point -- 'no tracked report files': it enumerates tracked outputs with `git ls-files`, and an archive extract carries no git index; in a clone of the same commit it runs and passes"
  ],
  "with_manuscript_visible": {
   "collected": 91,
   "passed": 90,
   "failed": 0,
   "skipped": 1,
   "skip_is": "the git-index coverage check above; it is the only test that cannot run from an archive (VARIANT_FM_MANUSCRIPT and VARIANT_FM_SUPPLEMENT both set)"
  },
  "in_a_clone_with_manuscript_visible": {
   "collected": 91,
   "passed": 91,
   "failed": 0,
   "skipped": 0
  }
 },
 "bare_clone_note": "The bare-clone measurement below is the frozen-matrix-v1 release's (2026-09-09) and is kept for the record; the manuscript now discloses the archive measurement."
}
